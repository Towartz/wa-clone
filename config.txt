# ==============================================================================
# WhatsApp Clone Tool - External Build Tools Configuration
# ==============================================================================
# Leave paths empty or commented out to automatically detect from Environment/PATH.

# Path to Apktool (.jar or executable script)
APKTOOL_PATH=tools\apktool.jar

# Path to Android SDK zipalign executable
ZIPALIGN_PATH=C:\Android\build-tools\35.0.0\zipalign.exe

# Path to Android SDK apksigner (.jar, .bat, or executable)
APKSIGNER_PATH=tools\apksigner.jar

# Auto-sign repacked APK with apksigner (default: false - outputs unsigned APK)
AUTO_SIGN=false

# Keystore configuration for signing cloned APKs (when --sign or AUTO_SIGN=true is used)
KEYSTORE_PATH=towartz.jks
KEYSTORE_PASS=pass:towartz123
KEYSTORE_ALIAS=towartz

# Default worker thread count for parallel Smali/XML processing
DEFAULT_WORKERS=12

# Output folder for final cloned APKs
OUTPUT_DIR=dist

# Automatically clean up intermediate build files (*_unsigned.apk, staging folders)
AUTO_CLEAN_BUILD=true

# Single canonical decompiled folder name (prevents duplicate decompiled_* folders)
DECOMPILED_DIR=decompiled_base

# Check for tool updates (Apktool, APKEditor) on launch
CHECK_TOOL_UPDATES=false
